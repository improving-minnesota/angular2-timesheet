/**
 * TODO: Create a new class named EmployeeService that is decorated as injectable. Add a constructor dependency on our
 * ExtHttp service. Add a method named getEmployees that uses the ExtHttp service to retrieve a list of employees and
 * returns and Observable that emits an Employee list.
 *
 * 1. Import dependencies for decorators, RxJS, and constructor dependencies.
 * 2. Create a new class named EmployeeService that is decorated as Injectable.
 * 3. Add a constructor that pulls in our required dependencies.
 * 4. Add a getEmployees method that returns an Observable that emits Employee instances.
 */

// TODO: #1
// TODO: #2
// TODO: #3
// TODO: #4
