import { NgModule } from '@angular/core';

// import new dependencies

@NgModule({
  declarations: [
    // declare any new components
  ],
  imports: [
    // import any modules that you require
  ],
  exports: [
    // export any shared components
  ],
  providers: [
    // provide services for use within this module
  ],
})
export class EmployeeModule { }
