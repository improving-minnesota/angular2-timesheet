/**
 * TODO: Create a new Angular module using the @NgModule decorator using app.module.ts as a reference.
 *
 * 1. Import required Angular dependencies as well as thew newly created project list component.
 * 2. Create a new module class decorated with @NgModule. Add project list component to the component configuration.
 */

// TODO #1

// TODO #2
